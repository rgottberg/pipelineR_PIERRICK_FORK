# globals.R

#' @keywords internal
utils::globalVariables(c(
  "high", "low", "volume", "close_adjusted",
  "index_ts", "metric", "value",
  "batch_id", "symbol", "status", "n_rows", "timestamp",
  "adjusted"
))
